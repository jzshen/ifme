<%= form_for(@ally_permission) do |f| %>
  <% if @ally_permission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ally_permission.errors.count, "error") %> prohibited this ally_permission from being saved:</h2>

      <ul>
      <% @ally_permission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
